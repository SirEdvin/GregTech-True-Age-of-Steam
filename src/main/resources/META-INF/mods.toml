modLoader = "javafml"
loaderVersion = "[47,)"
license = "${modLicenseVersion}"

# This is a URL to e.g. your GitHub or CurseForge issues page.
# It will appear in any crash reports this mod is directly involved in.
# issueTrackerURL="https://github.com/invalid/pleasechangeme/issues" #optional
# A list of mods - how many allowed here is determined by the individual mod loader

[[mods]]
    modId = "${modIdVersion}"
    version = "${version}"
    displayName = "${modNameVersion}"
    displayURL = "${modUrlVersion}"
    # This will be displayed as your mod's logo in the Mods panel.
     logoFile="pack.png" #optional
    # Some more fluff displayed in the Mods panel. Feel free to issue your special thanks here!
    # credits="Thanks to Mojang for making this great game" #optional
    # Some more fluff displayed in the Mods panel. Plug your stuff here!
    authors="${modAuthorVersion}"
    description = '''
Mod that tries to make Steam power generation great for use!
'''

    [[dependencies.${modIdVersion}]]
        modId = "forge"
        mandatory = true
        versionRange = "[${forgeMajorVersion},)"
        ordering = "NONE"
        side = "BOTH"
    [[dependencies.${modIdVersion}]]
        modId = "minecraft"
        mandatory = true
        versionRange = "[${minecraftVersion},)"
        ordering = "NONE"
        side = "BOTH"
    [[dependencies.${modIdVersion}]]
        modId = "gtceu"
        mandatory = true
        versionRange = "[${gtceuVersion},)"
        ordering = "AFTER"
        side = "BOTH"
